services:
  - type: web
    name: loan-backend
    env: python
    region: oregon
    buildCommand: pip install flask flask_cors gunicorn
    startCommand: gunicorn app:app --bind 0.0.0.0:10000
    workingDir: backend
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
    plan: free
